# ============================================
# TeslamateCyberUI 环境变量配置示例
# 复制此文件为 .env 并修改配置
# ============================================

# ------------------------------------------
# 必填配置 - TeslaMate 数据库连接
# ------------------------------------------
# 数据库主机地址（TeslaMate PostgreSQL 所在服务器）
TESLAMATE_DB_HOST=192.168.1.100

# 数据库端口（默认 5432）
TESLAMATE_DB_PORT=5432

# 数据库用户名
TESLAMATE_DB_USER=teslamate

# 数据库密码
TESLAMATE_DB_PASSWORD=your_password_here

# 数据库名称（默认 teslamate）
TESLAMATE_DB_NAME=teslamate

# SSL 模式（disable / require）
TESLAMATE_DB_SSLMODE=disable

# ------------------------------------------
# 可选配置 - 服务设置
# ------------------------------------------
# CyberUI 访问端口（默认 8080）
CYBERUI_PORT=8080

# 运行模式（debug / release）
CYBERUI_SERVER_MODE=release

# 日志级别（debug / info / warn / error）
LOG_LEVEL=info

# 时区设置
TZ=Asia/Shanghai

# ------------------------------------------
# 可选配置 - API 密钥
# ------------------------------------------
# CyberUI API 密钥（用于 API 认证，留空则不启用认证）
CYBERUI_API_KEY=

# ------------------------------------------
# 可选配置 - Mock 数据
# ------------------------------------------
# 是否启用 Mock 数据（脱离 TeslaMate 数据库运行，用于演示或开发测试）
# 默认: false (使用真实数据库)，设置为 true 则使用本地数据
CYBERUI_MOCK_DATA=false
